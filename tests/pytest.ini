[pytest]
markers =
    unit: fast, isolated tests for individual functions.
    integration: tests for component interactions, requires services like DB/Redis.
    e2e: full end-to-end tests against synthetic web services.
    browser: tests that require a headless browser instance.